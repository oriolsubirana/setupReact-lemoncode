import * as React from 'react';
import { Link } from 'react-router-dom';

export const HotellCollectionScene = () => {
    return (
        <>
            <h2>Hello from hotel collection page</h2>
            <Link to="/" >Navigate to login collection</Link>
        </>
    );
}